@model IEnumerable<BloodDonationAPI.Models.DTOs.BloodDonationEventDto>
@{
    ViewData["Title"] = "Sự Kiện Hiến Máu Nhân Đạo";
}

<div class="container py-5">
    <h1 class="text-center mb-2" style="color: #d32f2f;">
        <i class="fa fa-heart"></i> <b>Sự Kiện <span class="text-danger">Hiến Máu Nhân Đạo</span></b>
    </h1>
    <p class="text-center mb-4 fs-5 text-muted">
        Tham gia các sự kiện hiến máu để cứu giúp những người cần được truyền máu cấp cứu. Mỗi đơn vị máu có thể cứu sống 3 người.
    </p>

    <div class="row justify-content-center mb-4">
        <div class="col-md-8">
            <form method="get" class="d-flex gap-2">
                <input type="text" name="search" class="form-control" placeholder="Tìm kiếm sự kiện hoặc địa điểm..." value="@Context.Request.Query["search"]" />
                <select name="filter" class="form-select" style="max-width: 180px;">
                    <option value="">Tất cả</option>
                    <option value="ongoing">Đang diễn ra</option>
                    <option value="upcoming">Sắp diễn ra</option>
                </select>
                <button type="submit" class="btn btn-danger"><i class="fa fa-search"></i></button>
            </form>
        </div>
    </div>

    <div class="row g-4">
        @if (Model != null && Model.Any())
        {
            foreach (var evt in Model)
            {
                <div class="col-md-6 col-lg-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-header bg-white border-0 d-flex justify-content-between align-items-center">
                            <span class="badge bg-success">@evt.Status</span>
                        </div>
                        <div class="card-body">
                            <h5 class="card-title fw-bold">@evt.Title</h5>
                            <p class="card-text text-muted">@evt.Description</p>
                            <ul class="list-unstyled mb-2">
                                <li><i class="fa fa-calendar-alt text-danger me-2"></i> @evt.StartDate.ToString("dd/MM/yyyy") - @evt.EndDate.ToString("dd/MM/yyyy")</li>
                                <li><i class="fa fa-map-marker-alt text-danger me-2"></i> @evt.LocationName</li>
                                <li><i class="fa fa-location-dot text-danger me-2"></i> @evt.Address</li>
                                <li><i class="fa fa-users text-danger me-2"></i> @evt.CurrentQuantity/@evt.TargetQuantity người đăng ký</li>
                            </ul>
                            <div class="mb-2">
                                <span class="badge bg-light text-danger border border-danger">
                                    Địa điểm: <b>@evt.LocationName</b>
                                </span>
                            </div>
                            <a asp-controller="BloodDonationEvent" asp-action="Register" asp-route-id="@evt.Id" class="btn btn-danger w-100 mt-2">
                                Đăng ký tham gia
                            </a>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12 text-center text-muted">
                Không có sự kiện nào phù hợp.
            </div>
        }
    </div>
</div> 